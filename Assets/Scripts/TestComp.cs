using EditorWindows;
using UnityEngine;

namespace DefaultNamespace
{
    public class TestComp : MonoBehaviour
    {
        public MapNode node = new MapNode();
        public Map map = new Map();
    }
}